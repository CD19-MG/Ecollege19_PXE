<?xml version="1.0" encoding="utf-8"?>
<!--
  generalize.xml - passe a sysprep (/unattend) par Preparer-la-capture.cmd.
  SkipRearm=1 : sysprep /generalize NE consomme PAS le compteur de "rearm" d'activation
  (limite ~3 par defaut) -> on peut re-syspreper un MASTER autant de fois qu'on veut
  (mise a jour du master puis re-capture). Aucun secret ici (versionnable).
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend">
  <settings pass="generalize">
    <component name="Microsoft-Windows-Security-SPP" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <SkipRearm>1</SkipRearm>
    </component>
  </settings>
</unattend>
